<div class="index-container">
  <h1>Users</h1>
  <ul>
    <% @users.each_with_index do |user, index| %>
      <div class="ul-container">
        <p style="font-size: 1.25rem; font-weight: 600"><%= index+1 %>)</p>
        <div style="display: flex; flex-direction: column; height: 100%; justify-content: space-between">
          <div class="list-info">
            <li class="name-text"><%= user.first_name %> <%= user.last_name %></li>
            <li><%= user.birthday %></li>
            <li><%= user.gender %></li>
            <li><%= user.email %></li>
            <li><%= user.phone %></li>
            <li><%= user.subject %></li>
          </div>
          <p><%= link_to "Delete", user_path(user), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },class: "destroy-button" %></p>
        </div>
      </div>
    <% end %>
  </ul>
</div>